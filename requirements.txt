# Runtime -- pinned per Part 10 SS2.2 (Spark 3.5 LTS line = EMR 7.x / Glue 5.0)
pyspark==3.5.5
pyyaml==6.0.2
boto3==1.34.162
kafka-python==2.0.2

# Dev / test (the Stage-0 gate tooling)
pytest>=8.0
pytest-cov>=5.0
ruff>=0.5
black>=24.0
chispa>=0.10
moto[s3]>=5.0
pre-commit>=3.7

# Stage 1 additions (uncomment when the stage starts)
# dbt-core==1.9.*
# dbt-athena-community==1.9.*
# great-expectations==0.18.*

# Install this project itself in editable mode (must stay LAST)
-e .
